<% require "./lib/greader" -%>
Gem::Specification.new do |s|
  s.name = "greader"
  s.version = "<%= GReader.version %>"
  s.summary = %{Google Reader API client.}
  s.description = %Q{It reads Google Reader feeds.}
  s.authors = ["Rico Sta. Cruz"]
  s.email = ["rico@sinefunc.com"]
  s.homepage = "http://github.com/rstacruz/greader.rb"
  s.files = <%= Dir["{lib,test}/**/*", "*.md", "Rakefile"].reject { |f| File.directory?(f) }.inspect %>

  s.add_dependency "nokogiri"
  s.add_dependency "rest-client", "~> 1.6"
end
